# Homebrew enterpise Tap
Home-brew tap that contains old/defunct versions of enterprise type tools

# Usage
To make this tap available to homebrew

    brew tap set321go/enterprise
    
install as normal.
